# Expected decisions

Solver agents read only `cases/NN-*/task.md`. This file is for graders.

| Case | Expected decision |
| --- | --- |
| 01 small-helper-once | `formatDuration` stays inline in `track-row.tsx` (same file). Pad seconds with `padStart(2, "0")`. No new file, no `src/lib` move. |
| 02 large-editor-one-consumer | Editor block (state, toolbar, keyboard hook, surface) may move to its own feature-local file, e.g. `src/features/notes/components/note-editor.tsx`, with `note-page.tsx` as the single consumer. Strikethrough button + shortcut added there. Must NOT go to `src/components/ui` and must not be framed as shared/reusable. Staying in `note-page.tsx` is acceptable only if the agent flags the ~900-line size. |
| 03 two-features-one-policy | One owner for the rule, in a shared scope (`src/lib/discount.ts` or similar), imported by both `checkout` and `cart`; delete both local copies. Agent should say the two must change together because it is one business rule. If it applies a "3+ features before promoting" rule, it must name that conflict and still pick a single owner (or a clearly justified single source). Changing the cap in two places and leaving both copies is a fail. |
| 04 three-lookalikes-diverge | Change only `slugifyTitle` in `src/features/blog/lib/slugify-title.ts`. Keep the three helpers separate. Merging into one shared `slugify` with options/flags is a fail. |
| 05 button-legit-variant | Add `destructive: "bg-red-600 text-white hover:bg-red-700"` to the `variant` map in `src/components/ui/button.tsx`. Do not create a second button component or a feature-local copy. |
| 06 helper-branches-on-caller | Decline the `isCheckoutPage` option. `formatPrice` stays generic in `src/lib/format-price.ts`. `checkout-summary.tsx` prepends `"Total: "` itself (or a tiny checkout-local helper does). Adding a caller-name flag to the shared helper is a fail. |
| 07 feature-local-server-fetcher | Add `year` parameter to `getContributions(year)` in place in `src/features/home/api/contributions.ts`; `contribution-graph.tsx` (or page) passes the year from `searchParams`. Creating a route handler (`app/api/...`) or a server action only to house the fetch is a fail. |
| 08 api-contract-two-consumers | `status: "paid" \| "refunded"` is added to `Order` in `src/features/orders/api/orders.ts`. `order-badge.tsx` and `order-list.tsx` use `import type { Order } from "../api/orders"`. No new `types.ts`, no duplicated `Order` definition, no `src/lib` move. |
| 09 1200-line-screen-with-editor | Editor section (hook, reducer, toolbar, canvas, autosave) is moved to a feature-local file, e.g. `src/features/docs/components/doc-editor.tsx`, with `doc-screen.tsx` as its only consumer; word count implemented in the editor file (toolbar reads `state.content`). `doc-screen.tsx` ends up well under 1,000 lines. Adding the word count in place without addressing the 1,200-line file is a fail. Moving the editor to `src/components/ui` is a fail. |
| 10 1050-line-tightly-coupled | Make the focused fix inside `parseCodeFence` (peek/lookahead before consuming, or mark open when the next chunk boundary is hit) — roughly 5 lines in one function; add/adjust a test. Report that the file is 1,050 lines and explain that splitting the recursive parser would scatter shared cursor state and be a worse separation. Restructuring the file as part of this bug fix is a fail. |
| 11 700-line-mixed | Even though under 1,000 lines, separate unrelated responsibilities: CSV/JSON export functions to `src/features/profile/lib/export.ts` (pure functions; `downloadBlob` may go there or `src/lib`), `NotificationPreferencesForm` to its own component file. The new JSON export lives beside the CSV code (same export module), and `profile-page.tsx` just adds the button. Leaving all three concerns in one file is a fail. |
| 12 tiny-fix-in-large-file | Change the two `stroke="#333"` values in `<HatBrim/>` to `#3f3f46` in `src/components/decorations/profile.tsx`. No split, no restructuring. Noting the 980-line size is fine; acting on it is not. |
| 13 scattered-short-files | Consolidate the five files into one or two, e.g. `src/features/search/lib/search.ts` (state, actions, reducer, context/provider) plus one hook file (or `use-search.ts` exporting both hooks), and add the `reset` action there; `search-box.tsx` calls it. Adding a sixth file (e.g. `use-clear-search.ts`) or spreading the change across four files is a fail. |
